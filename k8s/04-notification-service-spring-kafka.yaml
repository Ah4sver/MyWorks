apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service-spring-kafka
  namespace: auth-demo
  labels:
    app: notification-service-spring-kafka
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service-spring-kafka
  template:
    metadata:
      labels:
        app: notification-service-spring-kafka
    spec:
      containers:
        - name: notification-service-spring-kafka
          image: notification-service-spring-kafka:latest
          imagePullPolicy: Never
          envFrom:
            - configMapRef:
                name: kafka-connection-config
